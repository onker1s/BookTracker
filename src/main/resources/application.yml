spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booktracker?useSSL=false&serverTimezone=UTC
    username: root
    password: 454529
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # или create-drop / validate / none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
